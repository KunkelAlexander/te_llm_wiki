---
title: "Agent instructions — T&E LLM wiki"
audience: any agentic AI system maintaining this wiki (Claude Code today, others later)
---

# Purpose

This wiki turns T&E's raw publications — one row per document in `output/metadata_with_fulltext.parquet`,
produced by a separate scrape/parse pipeline that isn't part of this repo — into a small set of
markdown pages centered on **subjects** — the things T&E works on (fuels, vehicle
segments, policy dossiers, specific technologies or feedstocks) — rather than one page per
publication. This follows Karpathy's llmwiki pattern: pages synthesize evidence across many
sources and are updated incrementally as new publications arrive, rather than being generated
once from a fixed snapshot.

Model this on a real encyclopedia, not a set of report summaries. A subject page name should be a
noun a reader would type into search — "Biofuels", "Zero-emission trucks", "Vans" — not a sentence
describing an argument. Within a broad subject page, organize prose under `##` subsections for its
sub-themes (e.g. a "Biofuels" page has a "## Fraud and mislabeling" subsection). When one
subsection accumulates enough independent material to be worth its own deep-dive — a specific
feedstock, technology, or regulation, e.g. "POME" — split it into its own page and leave a
one-line pointer under the parent subsection (Wikipedia's "Main article: X" hatnote pattern). Link
back the other way too, so the two pages form a hub-and-child pair.

**Do not create pages for people.** Authors, analysts, and expert-group contributors are cited
inline in prose or in a source page's frontmatter, never as their own wiki page — a wiki page per
staff member does not scale and isn't what a reader searching a subject wiki wants. If it's
genuinely useful to note who tends to work on a subject, one sentence in that subject's page is
enough.

**The goal is not full coverage of the input documents.** Most sentences in a T&E report are not
wiki-worthy. Capture only:
- Recurring arguments and positions T&E has taken (e.g. "LNG trucks do not reduce emissions")
- Quantitative estimates (emissions figures, cost estimates, market projections)
- Named policies/dossiers T&E advocates on or against
- Specific technologies, feedstocks, or fuel types worth their own page once they recur

If in doubt, leave it out. A page that's too short but accurate is better than one padded with
paraphrased filler.

# Directory layout

```
wiki/
  sources/          one markdown file per source document — regenerated by 1_build_wiki_subset.py
                     (or its full-corpus successor). Treat as read-only reference material.
  sources/index.md   cross-reference manifest — regenerated by 2_refresh_wiki_index.py, do not
                     hand-edit (see "Index refresh" below)
  topics/            one markdown file per topic/entity — hand-authored and maintained by the
                     agent. These are the actual wiki content; they evolve over time.
  topics/index.md    cross-reference manifest — regenerated by 2_refresh_wiki_index.py, do not
                     hand-edit (see "Index refresh" below)
  topics/DEDUP_INDEX.md   maintainer-only cross-reference table (which topic cites which source,
                     first/last seen dates) — regenerated alongside topics/index.md but excluded
                     from the built site via _config.yml; topics/index.md itself is the public
                     page and stays a plain topic/type/summary table
  AGENT_INSTRUCTIONS.md   this file
```

## Source pages (`wiki/sources/*.md`)

Frontmatter: `id, title, date, office, publication_type, article_url, pdf_url, doc_ids`.
Body: a short citation card — publication type/office/date line, a link to the original
publication (`article_url`, plus `pdf_url` if it differs), and the publication's own
summary/abstract as a blockquote (or a "no abstract available" note if none exists). The body
deliberately does **not** include the cleaned fulltext dump: PDF-extracted body text comes out as
running headers, footnote markers and empty table cells mashed together, which reads as a broken
scrape rather than a citation a reader can trust — see `1_build_wiki_subset.py` for the exact
template. The fulltext itself still lives in `output/metadata_with_fulltext.parquet` for
search/embedding; it's just not rendered on the page. These pages are regenerated by a script
(never hand-edited) whenever the upstream filter/corpus changes — don't rely on their exact
filenames staying stable across regenerations if the underlying title changes; re-check links
after regenerating.

## Topic pages (`wiki/topics/*.md`)

Frontmatter:
```yaml
---
title: "<subject name, a noun — e.g. 'Biofuels', 'POME'>"
type: topic | concept | policy
summary: >
  One or two sentences summarising what this page covers — this is what shows up in
  topics/index.md, so an agent scanning it can tell if a new source belongs here without
  opening the file.
first_seen: <ISO date of earliest cited source>
last_updated: <ISO date of most recent cited source>
---
```
Use `type: topic` for a broad subject/hub page, `type: concept` for a narrower page split out
from a hub's subsection (e.g. POME split out of Biofuels), and `type: policy` for a page centered
on a specific regulation/dossier. There is no `person` type — see "Do not create pages for people"
above.

Body: prose organized so it reads as a living summary, not a changelog dump. When an estimate or
position has evolved, say so explicitly and in date order, e.g.:

> T&E's 2021 [LNG trucks: a dead-end bridge](sources/lng-trucks-a-dead-end-bridge.md) report
> found real-world LNG truck emissions were no better than diesel on a well-to-wheel basis. By
> 2023, [Ready or not](sources/ready-or-not-who-are-the-frontrunners-in-the-global-race-to-clean-up-trucks.md)
> reiterated this position while adding that battery-electric trucks had meanwhile become
> commercially available from multiple manufacturers.

(Note: topic pages themselves live in `wiki/topics/`, so from *there* the correct relative path
back to a source page is `../sources/<filename>.md` — the example above is written from this
top-level instructions file, one directory higher.)

For a hub page with a spun-off concept page (both live side by side in `wiki/topics/`, so the
relative link between them is just the filename, no `../`), use a one-line hatnote under the
relevant subsection:

> ## Fraud and mislabeling
> *Main page: [POME](topics/pome.md).* Reported volumes of several "waste" feedstocks exceed
> plausible production potential, most notably palm oil mill effluent (POME) — see POME for the
> detailed figures. [...]

and open the concept page itself with a pointer back up: *Part of [Biofuels](topics/biofuels.md).*
(paths above are written relative to this instructions file at `wiki/`; from inside
`wiki/topics/pome.md` itself the link is just the bare filename `biofuels.md`, as in the real
example at `wiki/topics/pome.md` and `wiki/topics/biofuels.md`).

## Citation format

Every non-trivial claim links to the source page it came from, using a relative markdown link:
`[Short label](../sources/<filename>.md)`. Never cite a claim without a source link. If a claim
draws on multiple sources, cite each. Do not link to the live `article_url`/`pdf_url` directly from
topic-page prose — those belong in the source page's frontmatter; topic pages link to the local
source page, which in turn carries the original URL.

# Update workflow

When new source pages appear (after re-running the extraction script over more documents):

1. **Always start here**: read `wiki/topics/DEDUP_INDEX.md` (maintainer-only, excluded from the
   built site) — it's a generated table of every topic, its one-line summary, and every source it
   already cites. Use it to see at a glance which topics are plausibly relevant to a new source
   before reading anything else. Check `wiki/sources/index.md` too — its "cited by" column shows
   whether a given source is already covered anywhere, which flags sources nobody has processed
   yet (empty "cited by").
2. For each new/unprocessed source, read it and identify which existing topics it touches
   and whether it introduces a new topic worth a page.
3. Update matching topic pages: add a dated entry if it changes/extends an estimate or position;
   otherwise add a citation if it simply corroborates an existing point. Don't duplicate content
   that's already well covered — a second citation for an unchanged point is enough.
4. Create a new topic page only when the material doesn't fit any existing page and is itself
   substantial (i.e. would come up in multiple future publications, not a one-off mention). Give
   it a `summary` in its frontmatter (see above). If a subsection of an existing hub page has
   grown large enough to stand alone (recurs across several sources with its own figures and
   arguments), split it into a `concept` page and leave a hatnote behind, per the pattern above.
5. Update `last_updated` in the frontmatter of any topic page you touched.
6. Run `python 2_refresh_wiki_index.py` from the repo root to regenerate both index.md files from
   the citations/frontmatter you just wrote. Never hand-edit `topics/index.md` or
   `sources/index.md` — they're overwritten on every run.

# Scaling to the full corpus

This process and directory layout do not change when the "Simon Suzan" test filter is dropped and
the extraction script runs over the full `metadata_with_fulltext.parquet` — only the volume of
source pages and the number of passes through the update workflow above increases. A future
automated system can run steps 1-3 of the update workflow as a scheduled batch job; the level-of-
detail judgment calls (steps 3-4) are the part that most needs a competent agent rather than a
fixed script.
